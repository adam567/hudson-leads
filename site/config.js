// Browser-side config. Edit these two values once your Supabase project exists.
// Both are safe to expose publicly — RLS in Postgres is the actual security boundary.
window.HUDSON_LEADS_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR_ANON_KEY"
};
